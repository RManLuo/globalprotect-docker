services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    network_mode: "service:globalprotect"
    depends_on:
      - globalprotect
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN:?set TUNNEL_TOKEN to your Cloudflare tunnel token}
